import 'package:flutter/material.dart';

class ColorsTheme {
  static const primary = Color.fromARGB(255, 42, 95, 240);
  static const primaryLight = Color.fromARGB(255, 42, 95, 240);
  static const primaryDark = Colors.black;
}
